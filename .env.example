# ========================================
# CV-Enhancer Environment Configuration
# ========================================
# Copy this file to .env.local (for local dev) or .env.kaggle (for cloud)

# ----------------
# Mode Configuration
# ----------------
MODE=local  # Options: local, kaggle, cloud

# ----------------
# LLM Configuration
# ----------------
LLM_PROVIDER=ollama  # Options: ollama, openai, anthropic, gemini
LLM_MODEL=llama3:8b  # Model name for the selected provider
LLM_API_KEY=         # Required for openai, anthropic (not needed for ollama, gemini)
LLM_API_BASE=http://localhost:11434  # For Ollama, set the API base URL
LLM_TEMPERATURE=0.7  # Temperature for LLM responses (0.0-1.0)
LLM_MAX_TOKENS=2048  # Maximum tokens for LLM responses

# ----------------
# Storage Configuration
# ----------------
STORAGE_TYPE=local   # Options: local, gcs
DATA_DIR=./data      # Local storage directory (used when STORAGE_TYPE=local)

# GCS Storage (only needed when STORAGE_TYPE=gcs)
GCS_BUCKET_UPLOADS=cv-enhancer-uploads
GCS_BUCKET_OUTPUTS=cv-enhancer-outputs

# ----------------
# GCP Configuration
# ----------------
# Only needed for Gemini LLM or GCS storage
GCP_PROJECT_ID=your-gcp-project-id
GCP_REGION=us-central1

# ----------------
# Vector Database Configuration
# ----------------
VECTOR_DB_TYPE=faiss  # Options: faiss, vertex
VECTOR_DB_PATH=./data/embeddings  # Path for FAISS index (used when VECTOR_DB_TYPE=faiss)

# ----------------
# Firestore Configuration
# ----------------
# Only needed for cloud deployments using Firestore
FIRESTORE_DATABASE=(default)

# ----------------
# ADK Configuration
# ----------------
ADK_MODE=local       # Options: local, cloud
ADK_HOST=localhost   # Host for local ADK server
ADK_PORT=8000        # Port for local ADK server

# ----------------
# User Interaction
# ----------------
USER_INTERACTION_MODE=non-interactive  # Options: interactive, non-interactive
# - interactive: Prompt for user input via terminal (for local development)
# - non-interactive: Use LLM inference only (for Kaggle/cloud/CI)

# ----------------
# Logging
# ----------------
LOG_LEVEL=INFO       # Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
